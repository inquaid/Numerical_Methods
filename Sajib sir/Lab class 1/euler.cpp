#include <bits/stdc++.h>
using namespace std;

int main() {
    string eqn;
    getline(cin, eqn);
    // cout << eqn;
    int n = eqn.size(), i = 0;
    while (i < n) {
        // cout << eqn[i];
        
        i++;
    }
}